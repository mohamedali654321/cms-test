<div class="row">
    <div class="container">
        <div class="col-md-10">
            <table class="table table-bordered text-center text-white">
                <thead class="bg-primary">
                <tr>
                    <th>Id</th>
                    <th>Publisher</th>
                    
                    <th>Date</th>
                    <th>Comment Body</th>
                </tr>
                </thead>

                <tbody class="text-dark">
                {{#each comments}}
                    <tr>
                        <td>{{id}}</td>
                        <td style="width: auto;">{{user.fristName}} {{user.lastName}}</td>
                        
                        <td>{{date}}</td>
                        <td>{{body}}</td>
                    </tr>

                {{/each}}
                </tbody>


            </table>
        </div>
    </div>
</div>
